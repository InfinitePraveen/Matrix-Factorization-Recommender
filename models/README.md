# Models

The notebook creates the following local artifacts:

- `svd_recommender.pkl` — trained Surprise SVD model.
- `movies.csv` — movie IDs and titles used by the Flask app.
- `user_ids.csv` — valid MovieLens user IDs.

These files are generated after running the notebook and are used by `app.py`.
